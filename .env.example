# Copiez ce fichier en `.env` puis adaptez (utilisé par docker compose).
# Choisissez des mots de passe forts avant tout déploiement réel.
MARIADB_ROOT_PASSWORD=change_me_root
DB_NAME=logflow
DB_USER=logflow
DB_PASS=change_me_logflow

# Port HTTP exposé sur la machine hôte
APP_PORT=8085
